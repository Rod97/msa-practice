server:
  port: 9296
  
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rod97/config-server-MSA/
          clone-on-start: true